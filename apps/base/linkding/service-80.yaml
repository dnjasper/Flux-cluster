apiVersion: v1
kind: Service
metadata:
  name: linkding-80
spec:
  selector:
    app: linkding  # Ensure this matches your app label
  ports:
    - protocol: TCP
      port: 80         # The external port
      targetPort: 9090  # Forward traffic to internal port 9090
  type: NodePort      # Expose service on a node port

